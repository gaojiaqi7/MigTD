name: Daily 4:45 Trigger

on:
  schedule:
    # Runs every day at 4:45 UTC
    - cron: '45 4 * * *'

jobs:
  run-task:
    runs-on: ubuntu-latest
    steps:
      - name: Example step
        run: echo "This workflow runs every day at 4:45 UTC"